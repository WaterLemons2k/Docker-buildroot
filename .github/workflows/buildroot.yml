name: Buildroot

on:
  push:

jobs:
  docker:
    uses: WaterLemons2k/Docker/.github/workflows/docker.yml@main
    with:
      tags: |
        waterlemons2k/buildroot
        waterlemons2k/buildroot:hello-world
    secrets:
      TOKEN: ${{ secrets.TOKEN }}
